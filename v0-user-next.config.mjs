/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuración para asegurar que los archivos estáticos se incluyan en la compilación
  output: 'standalone',
  // Otras configuraciones según sea necesario
};

export default nextConfig;

